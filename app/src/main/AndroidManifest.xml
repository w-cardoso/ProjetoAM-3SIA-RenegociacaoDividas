<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="renegociacao.moosegroup.com.br.renegociardividas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ui.TelaCadastroActivity" />
        <activity android:name=".Ui.MainActivity" />
        <activity android:name=".Ui.TelaInicialActivity" />
        <activity android:name=".Ui.MenuActivity" />
        <activity android:name=".Ui.CadastrarDividasActivity" />
        <activity android:name=".Ui.ParceirosActivity"></activity>
    </application>

</manifest>